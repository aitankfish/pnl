/**
 * useNetwork Hook
 *
 * Re-export of the network context hook for backward compatibility.
 * This ensures existing code continues to work.
 */

export { useNetwork, type SolanaNetwork } from '../contexts/NetworkContext';
